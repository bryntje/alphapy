[build]
builder = "Dockerfile"
dockerfilePath = "Dockerfile.railway"

[deploy]
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"

# Next.js dashboard service for Alphapy Discord bot
# Serves the web configuration interface at alphapy.innersync.tech
