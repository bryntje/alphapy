[tool.ruff]
line-length = 120
target-version = "py311"
extend-exclude = ["node_modules", "alembic/versions"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]

[tool.pytest.ini_options]
addopts = "-v --maxfail=1 --cov=. --cov-report=term-missing"
testpaths = ["tests"]
